function countZeroDigit(a, b) {
  let arr = new Array();
  for (let i = a; i <= b; i++) {
    arr.push(i);
  }
  // for (let i = 0; i < arr.length; i++) {
  //   if (arr[i] )
  // }
  console.log(arr);
}
console.log(countZeroDigit(100, 120));
